const startQuiz = document.getElementById('startScreen');
const quizing = document.getElementById('quizing');
const quizResult = document.getElementById('resultScreen');
const quizContainer = document.getElementById('quiz-container');

quizContainer.innerHTML = startQuiz.innerHTML;
